export enum EventTypes {
    Unknown,
    Pageload,
    Custom
};

export enum PlatformTypes {
    Unknown,
    Windows,
    Mac,
    Linux,
    Android,
    iOS
};

export enum BrowserTypes {
    Unknown,
    IE,
    Edge,
    Chrome,
    Safari,
    Opera,
    Firefox
};